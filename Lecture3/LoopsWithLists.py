#In majority, for loops are used with lists
idx=0
nums=[10,20,30,40]
x=30
for val in nums:
    if(val==x):
        print(f"X found at idx={idx}")
    idx+=1


#Linear search : searched indexwise in sequence